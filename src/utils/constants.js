const API_URL = 'https:/api.platform-20.com:3000/'
export const API_ROUTES = {
  SIGN_IN: `${API_URL}/api/user/login`,
  GET_USER: `${API_URL}/posts/authorize`,
}

export const APP_ROUTES = {
  SIGN_IN: '/login',
  DASHBOARD: '/',
}